server:
  servlet:
    context-path: /myhouse24-amirb-nikitaf
  port: 8081

spring:
  application:
    name: My_House_24
  datasource:
    url: 'jdbc:mysql://localhost:3306/my_house_24'
    username: 'user'
    password: 'user'
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database: mysql
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

upload:
  path: "C:/Users/Amir Banov/files"

